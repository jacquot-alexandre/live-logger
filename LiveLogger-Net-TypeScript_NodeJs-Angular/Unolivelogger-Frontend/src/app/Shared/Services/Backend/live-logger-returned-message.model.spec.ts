import { LiveLoggerReturnedMessage } from './live-logger-returned-message.model';

describe('LiveLoggerReturnedMessage', () => {
  it('should create an instance', () => {
    expect(new LiveLoggerReturnedMessage()).toBeTruthy();
  });
});
